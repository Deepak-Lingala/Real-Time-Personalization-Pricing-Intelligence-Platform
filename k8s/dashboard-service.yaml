apiVersion: v1
kind: Service
metadata:
  name: pricing-intelligence-dashboard
  namespace: personalization-pricing
  labels:
    app.kubernetes.io/name: pricing-intelligence-dashboard
    app.kubernetes.io/part-of: real-time-personalization-pricing-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pricing-intelligence-dashboard
  ports:
    - name: http
      port: 8501
      targetPort: http
